[build]
# Use the Dockerfile in the repo root
builder = "dockerfile"

[deploy]
# Run the bot as a long-running worker process (not a web server)
startCommand = "python scripts/run_bot.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
